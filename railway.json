{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'python -m uvicorn backend.app.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
  }
}
